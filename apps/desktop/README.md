# Desktop Explorer (Prototype)

This is a lightweight React desktop UI prototype for browsing vocabulary data from sidecar APIs.

## Run

```bash
cd apps/desktop
npm install
npm run dev
```

Requires sidecar running at `http://127.0.0.1:17321`.
